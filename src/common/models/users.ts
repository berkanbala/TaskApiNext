export interface IUser {
  firstName: string;
  id: string;
  lastName: string;
  picture: string;
  title: string;
}
export interface IAllUsers {
  firstName: string;
  id: string;
  lastName: string;
  picture: string;
  title: string;
}
